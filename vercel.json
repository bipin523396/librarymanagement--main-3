{
  "buildCommand": "cd project_code && python -m pip install -r requirements.txt",
  "outputDirectory": "project_code",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/project_code/bookhub_backend/wsgi.py"
    }
  ]
}
